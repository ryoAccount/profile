{
  "name": "ryo-cv",
  "version": "1.0.0",
  "description": "Hello, I'm RYO. Welcome to My CV.",
  "keywords": ["cv"],
  "license": "MIT",
  "author": "RYO",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lighthouse": "lhci autorun --collect.numberOfRuns=1"
  },
  "devDependencies": {
    "node-forge": ">=0.10.0",
    "parcel-bundler": "^1.12.4"
  }
}
